/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Jarvis-test-',
  images: { unoptimized: true },
  reactStrictMode: true,
};

export default nextConfig;
